/* Backend CMD: Express*/
npm init -y
npm install express pg cors dotenv body-parser express-validator
node server.js


/* Frontend CMD: React*/

npx create-react-app frontend
npm install axios react-router-dom

// If directory not available
mkdir "C:\Users\syfur.shohag\AppData\Roaming\npm-cache"

npm start       

If Navbar not found in App.JS
npm install react-bootstrap bootstrap

Install sweetalert
npm install sweetalert2

If "npm.ps1 cannot be loaded because running scripts is disabled on this system." error raised, Run in Powershell "Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned"

If "'react-scripts' is not recognized as an internal or external command, operable program or batch file", Delete node_modules and package-lock.json. Then run again
npm install
npm start